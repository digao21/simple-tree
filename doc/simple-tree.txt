*simple-tree.txt*       A lightweight, asynchronous filesystem tree explorer for Neovim

==============================================================================
CONTENTS                                                  *simple-tree-contents*

    1. Introduction ............................... |simple-tree-intro|
    2. Commands ................................... |simple-tree-commands|
    3. Lua API .................................... |simple-tree-api|
    4. Buffer & Window Characteristics ............ |simple-tree-window|
    5. Tree Presentation & Filtering .............. |simple-tree-presentation|
    6. Navigation & Keybindings ................... |simple-tree-navigation|
    7. Highlights & Styling ....................... |simple-tree-highlights|

==============================================================================
1. INTRODUCTION                                              *simple-tree-intro*

Simple Tree is a clean, non-blocking filesystem tree explorer designed for
Neovim with a decoupled Hexagonal Architecture.

==============================================================================
2. COMMANDS                                               *simple-tree-commands*

:SimpleTree open                                            *:SimpleTree-open*
    Opens the SimpleTree sidebar window anchored to the far left. If the
    window is already open, shifts input focus to it.

:SimpleTree close                                           *:SimpleTree-close*
    Closes the SimpleTree sidebar window if open. If already closed, completes
    silently as a graceful no-op.

:SimpleTree toggle                                         *:SimpleTree-toggle*
    Toggles the SimpleTree sidebar window: closes it if open, or opens it if
    closed.

==============================================================================
3. LUA API                                                     *simple-tree-api*

require("simple-tree").setup(opts)                        *simple-tree.setup()*
    Initializes the plugin.

require("simple-tree").open(callback)                      *simple-tree.open()*
    Opens or focuses the SimpleTree explorer window.

require("simple-tree").close()                            *simple-tree.close()*
    Closes the SimpleTree explorer window.

require("simple-tree").toggle(callback)                  *simple-tree.toggle()*
    Toggles the SimpleTree explorer window open or closed.

==============================================================================
4. BUFFER & WINDOW CHARACTERISTICS                       *simple-tree-window*

Buffer Attributes:
    - buflisted = false (Unlisted scratch buffer)
    - buftype = "nofile" (Non-file type, never prompts to save)
    - bufhidden = "wipe" (Wiped on window closure)
    - swapfile = false (No swapfile)
    - filetype = "simple-tree" (Dedicated syntax/ftplugin)
    - modifiable = false (Read-only protection)

Window Attributes:
    - Anchored far left as a vertical split (split = "left")
    - winfixbuf = true (Buffer pinning)
    - winfixwidth = true (Width preservation)
    - cursorline = true (Cursor line highlighting)
    - Gutter suppressed (number=false, relativenumber=false, signcolumn="no",
      foldcolumn="0")

==============================================================================
5. TREE PRESENTATION & FILTERING                    *simple-tree-presentation*

By default, the presentation layer filters out all filesystem nodes (both files
and directories) whose names begin with a dot (`.`), such as `.git`, `.vscode`,
`.gitignore`, or `.env`.

When a directory is filtered, all of its descendant files and subdirectories
are omitted from the tree output as well. Non-dot files and non-dot
directories continue to be rendered with their appropriate indentation levels.

Node Sorting:
    - Folders are always rendered before files at every hierarchy level.
    - Within directory and file groups respectively, items are sorted
      alphabetically in a case-insensitive order.

Folder & File Icons:
    - Collapsed/closed directory nodes display FOLDER_ICON_CLOSED ("").
    - Expanded directory nodes (node.expanded = true) display
      FOLDER_ICON_OPEN ("").
    - Resolves icons using `mini.icons` (supporting directory and file
      categories), with fallback to `nvim-web-devicons` for files.

==============================================================================
6. NAVIGATION & KEYBINDINGS                           *simple-tree-navigation*

Inside the `simple-tree` buffer, the following default keybindings are active:

<CR>                                                    *simple-tree-<CR>*
    Toggles the expansion state of the directory under the cursor (expands if
    collapsed, collapses if expanded). No-op on file nodes.

<Space>                                              *simple-tree-<Space>*
    Same as <CR>, toggles directory expansion under the cursor.

==============================================================================
7. HIGHLIGHTS & STYLING                                *simple-tree-highlights*

Simple Tree decorates buffer lines using Neovim Extmarks within a dedicated
namespace (`simple_tree_highlights`).

Default Highlight Groups:

SimpleTreeDirectory                                     *hl-SimpleTreeDirectory*
    Highlight group for directory names. Linked by default to |hl-Directory|.

SimpleTreeFile                                               *hl-SimpleTreeFile*
    Highlight group for file names. Linked by default to |hl-Normal|.

SimpleTreeFolderIcon                                   *hl-SimpleTreeFolderIcon*
    Highlight group for folder icons ("", ""). Linked by default to
    |hl-Directory|.

SimpleTreeIcon                                               *hl-SimpleTreeIcon*
    Highlight group for file and fallback icons. Linked by default to
    |hl-Normal|.

==============================================================================
vim:tw=78:ts=8:ft=help:norl:
